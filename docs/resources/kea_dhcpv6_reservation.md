---
page_title: "opnsense_kea_dhcpv6_reservation Resource - terraform-provider-opnsense"
subcategory: Kea
description: |-
  Configure DHCPv6 reservations for Kea.
---

# opnsense_kea_dhcpv6_reservation (Resource)

Configure DHCPv6 reservations for Kea.

## Example Usage

```terraform
resource "opnsense_kea_dhcpv6_reservation" "example" {
  subnet_id = opnsense_kea_dhcpv6_subnet.lan.id

  ip_address = "2001:db8::102"
  duid       = "00:03:00:01:00:25:96:12:34:55"

  hostname = "myhost.example.com"

  domain_search = [
    "example.com"
  ]

  description = "example IPv6 host"
}

resource "opnsense_kea_dhcpv6_subnet" "lan" {
  subnet      = "2001:db8::/64"
  description = "LAN IPv6"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `duid` (String) DHCP Unique Identifier (DUID) of the client.
- `ip_address` (String) IPv6 address to offer to the client.
- `subnet_id` (String) Subnet ID the reservation belongs to.

### Optional

- `description` (String) Optional description.
- `domain_search` (Set of String) Domain search list. Defaults to `[]`.
- `hostname` (String) Hostname to offer to the client. Defaults to `""`.

### Read-Only

- `id` (String) UUID of the reservation.

## Import

In Terraform v1.5.0 and later, use an [`import` block](https://developer.hashicorp.com/terraform/language/import) to import opnsense_kea_dhcpv6_reservation using the `id`. For example:

```terraform
import {
  to = opnsense_kea_dhcpv6_reservation.example
  id = "<opnsense-resource-id>"
}
```

Using `terraform import`, import opnsense_kea_dhcpv6_reservation using the `id`. For example:

```console
% terraform import opnsense_kea_dhcpv6_reservation.example <opnsense-resource-id>
```
