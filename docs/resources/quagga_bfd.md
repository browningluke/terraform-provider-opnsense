---
page_title: "opnsense_quagga_bfd Resource - terraform-provider-opnsense"
subcategory: Quagga
description: |-
  Manages Quagga BFD (Bidirectional Forwarding Detection) settings. This is a singleton resource that manages existing upstream configuration.
  Important: This resource must be imported before it can be managed:
  
  terraform import opnsense_quagga_bfd.bfd quagga_bfd
  
  After importing, you can manage the configuration with terraform apply. Running terraform destroy will remove the resource from state but will NOT modify the upstream configuration.
---

# opnsense_quagga_bfd (Resource)

~> **Singleton Resource** This resource manages existing upstream Quagga configuration that cannot be created or destroyed through Terraform. You **must** import it before managing. Running `terraform destroy` removes the resource from state only and does **not** modify the upstream configuration.

Manages Quagga BFD (Bidirectional Forwarding Detection) settings. This is a singleton resource that manages existing upstream configuration.

**Important:** This resource must be imported before it can be managed:
```bash
terraform import opnsense_quagga_bfd.bfd quagga_bfd
```

After importing, you can manage the configuration with `terraform apply`. Running `terraform destroy` will remove the resource from state but will NOT modify the upstream configuration.

## Example Usage

```terraform
# This is a singleton resource. Import it before managing:
# terraform import opnsense_quagga_bfd.bfd quagga_bfd

resource "opnsense_quagga_bfd" "bfd" {
  enabled = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `enabled` (Boolean) Enable the BFD daemon. BFD provides fast failure detection for routing protocols such as BGP and OSPF. Defaults to `false`.

### Read-Only

- `id` (String) Always set to `quagga_bfd`. Use this value when importing: `terraform import opnsense_quagga_bfd.bfd quagga_bfd`

## Import

This resource **must** be imported before it can be managed. The import ID is always the fixed string `quagga_bfd`.

In Terraform v1.5.0 and later, use an [`import` block](https://developer.hashicorp.com/terraform/language/import):

```terraform
import {
  to = opnsense_quagga_bfd.settings
  id = "quagga_bfd"
}
```

Using `terraform import`:

```console
% terraform import opnsense_quagga_bfd.settings quagga_bfd
```
