---
page_title: "opnsense_quagga_static Resource - terraform-provider-opnsense"
subcategory: Quagga
description: |-
  Manages the Quagga staticd (static routing daemon) settings. This is a singleton resource that manages existing upstream configuration.
  Important: This resource must be imported before it can be managed:
  
  terraform import opnsense_quagga_static.static quagga_static
  
  After importing, you can manage the configuration with terraform apply. Running terraform destroy will remove the resource from state but will NOT modify the upstream configuration.
---

# opnsense_quagga_static (Resource)

~> **Singleton Resource** This resource manages existing upstream Quagga configuration that cannot be created or destroyed through Terraform. You **must** import it before managing. Running `terraform destroy` removes the resource from state only and does **not** modify the upstream configuration.

Manages the Quagga staticd (static routing daemon) settings. This is a singleton resource that manages existing upstream configuration.

**Important:** This resource must be imported before it can be managed:
```bash
terraform import opnsense_quagga_static.static quagga_static
```

After importing, you can manage the configuration with `terraform apply`. Running `terraform destroy` will remove the resource from state but will NOT modify the upstream configuration.

## Example Usage

```terraform
# This is a singleton resource. Import it before managing:
# terraform import opnsense_quagga_static.static quagga_static

resource "opnsense_quagga_static" "static" {
  enabled = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `enabled` (Boolean) Enable the staticd daemon. staticd is required to manage static routes via the FRRouting stack. Defaults to `false`.

### Read-Only

- `id` (String) Always set to `quagga_static`. Use this value when importing: `terraform import opnsense_quagga_static.static quagga_static`

## Import

This resource **must** be imported before it can be managed. The import ID is always the fixed string `quagga_static`.

In Terraform v1.5.0 and later, use an [`import` block](https://developer.hashicorp.com/terraform/language/import):

```terraform
import {
  to = opnsense_quagga_static.settings
  id = "quagga_static"
}
```

Using `terraform import`:

```console
% terraform import opnsense_quagga_static.settings quagga_static
```
